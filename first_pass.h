#ifndef FIRST_PASS_H
#define FIRST_PASS_H
#include "structs.h"

/* This function performs the first pass of the assembler on a given file, then calls the second padd */
void start_pass(file_data *fs);

#endif
